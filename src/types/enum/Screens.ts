/**
 * Enum mapping the upper limits for each screen type in px.
 */
export enum Screens {
  Mobile = 599,
  Small = 768,
  TabletPortrait = 899,
  TabletLandscape = 1366,
  Desktop = 1799,
  BigDesktop = Infinity,
}
